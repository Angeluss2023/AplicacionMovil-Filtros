import 'package:flutter/material.dart';

class FiltrosPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Filtros'),
      ),
      body: Center(
        child: Text('Aquí podrás aplicar tus filtros a las imágenes.'),
      ),
    );
  }
}
